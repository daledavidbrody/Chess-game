services:
  - type: web
    name: chess-games
    runtime: docker
    plan: free
    healthCheckPath: /api/health
    envVars:
      - key: NODE_ENV
        value: production
      - key: DATA_DIR
        value: /data
    disk:
      name: chess-games-data
      mountPath: /data
      sizeGB: 1
